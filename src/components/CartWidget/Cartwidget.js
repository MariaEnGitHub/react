import React from 'react'
import { BsBasket } from "react-icons/bs";

export const CartWidget = ({}) => {
        return (
            <div>
                <BsBasket/>
               
            </div>
        )
    }